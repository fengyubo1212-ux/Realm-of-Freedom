## 一、整体架构（纯前端零后端）

plaintext

```
NFC标签(NTAG213) → https://xxx.github.io/?scene=desert
                    ↓
浏览器加载纯前端静态页面
├─ Three.js 渲染3D无限公路场景
├─ JS读取URL参数 ?scene=xxx 自动加载对应风景
├─ Touch 触控逻辑：滑动控制纸飞机
└─ 资源：贴图、音效、模型全部打包进静态资源
```

## 二、技术栈（纯前端不变）

- 构建工具：Vite
- 3D 渲染：Three.js
- 动画：GSAP
- 样式：TailwindCSS
- 托管：GitHub Pages（免费），备选：Vercel（国内访问更稳）

> 唯一注意：**所有业务逻辑不能依赖服务端接口**，场景配置全部写在前端 `config.js`。

## 三、纯前端方案对比：CloudCode VS GitHub 免费部署

表格

| 对比项       | CloudCode 静态托管               | GitHub Pages（纯前端）                          |
| ------------ | -------------------------------- | ----------------------------------------------- |
| 费用         | 免费额度有限，流量大可能产生费用 | 永久免费                                        |
| HTTPS        | 自带                             | 自带，满足 NFC 要求                             |
| 部署方式     | 手动上传 dist                    | git push 自动构建部署                           |
| 国内访问     | 访问速度较好                     | GitHub Pages 国内偶尔加载慢（解决方案下文给出） |
| 是否适合项目 | 前期调试样品                     | 长期线上正式发布                                |

## 四、项目完整执行计划书（适配 GitHub 纯前端部署）

### 阶段 1：本地开发环境搭建（1～2 天）

1. 初始化 Vite + Three.js 项目
2. 目录结构（和之前方案保持一致）

plaintext

```
src/
├── scenes/          # 各场景配置
├── components/      # 纸飞机、道路、天空模型
├── controls/        # 移动端触控逻辑
├── utils/           # URL参数解析、音效工具
├── assets/          # 贴图、音效、glb模型
├── config.js        # 场景清单 sceneId:名称、色调、贴图
└── main.js
```

1. vite.config.js 关键配置（适配静态托管）

js

运行

```
import { defineConfig } from 'vite'
export default defineConfig({
  // 如果你仓库名是 plane-nfc，必须配置 base
  base: '/plane-nfc/',
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})
```

> 重点：`base` 路径必须和你的 github 仓库名称匹配，否则资源 404！

### 阶段 2：核心 3D 交互开发（3～9 天）

和之前方案完全一致：

1. 基础 Three.js 场景：相机、雾效、光照、无限滚动公路
2. 纸飞机模型 + 飞行缓动动画
3. 移动端竖屏适配、单指滑动控制飞机
4. 无限远景循环生成（道路、远山、云层）

### 阶段 3：场景系统 & NFC 路由核心（10～14 天）

纯前端核心：URL 参数解析，不需要后端

```
https://你的名字.github.io/plane-nfc/?scene=desert
```

js

运行

```
// 纯前端读取参数示例代码
const urlParams = new URLSearchParams(window.location.search);
const sceneId = urlParams.get('scene') || 'desert';
// 根据sceneId读取config.js配置，加载对应风景
loadScene(sceneId);
```

不同配色挂件，写入不同 scene 参数链接，实现一碰打开对应风景。

配套功能：

- 加载动画、启动欢迎页
- 背景音乐开关
- 截图分享（html2canvas，纯前端生成图片）
- 禁止页面缩放，移动端兼容

### 阶段 4：GitHub 自动化部署配置（15～16 天）

两种部署方案任选其一：

#### 方案 A：GitHub Pages + GitHub Actions（推荐）

1. 代码上传 GitHub 公开仓库
2. 编写 Action 脚本，git push 自动执行 `npm run build`，将 dist 部署到 pages
3. 获取官方 HTTPS 地址：`xxx.github.io/仓库名`

#### 方案 B：Vercel 导入 GitHub 仓库（优先推荐商用）

- 直接导入 Github 仓库，自动识别 Vite 项目，一键部署
- 国内访问比 GitHub Pages 稳定，同样免费，支持自定义域名

> 如果你后续卖货，强烈建议用 Vercel，避免客户打不开页面。

### 阶段 5：硬件联调测试（17～18 天）

1. 复制部署完成的 HTTPS 链接

2. 使用 NFC Tools 写入 NTAG213 标签

3. 真机测试：iPhone / 安卓触碰自动跳转对应场景

   ​

   ⚠️ 避坑清单：

4. **绝对不能使用 http**，必须 HTTPS（GitHub Pages/Vercel 全部满足）

5. 链接不要存在多余空格、换行，写入 NFC 前清理

6. iPhone 锁屏 NFC 感应位置在手机顶部，灵敏度低于安卓

### 阶段 6：性能优化 & 定稿（19～20 天）

1. 贴图压缩、模型轻量化，防止低端手机卡顿
2. 资源预加载优化
3. 修复浏览器后台暂停、触控冲突 BUG
4. 输出文档：部署手册、NFC 链接批量对照表